<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDescriptionGrey</item>
        <item name="colorPrimaryDark">@color/colorBlackField</item>
        <item name="colorAccent">@color/colorViolet</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="EditTextName" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorControlNormal</item>
        <item name="colorControlActivated">@color/colorControlNormal</item>
    </style>
</resources>
